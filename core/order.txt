# This file contains a manually maintained order of building packages
# Top to bottom, left to right. Each "section" should be on one line
# Please also add description of each section when applicable

# Prequisite: cmake, openssl(-dev)

# Make sure you have qsc-apk ready before bootstrapping core
# Base
filesystem liblocale-shim libsysv-ipc-shim
# autotools
make libxcrypt perl m4 autoconf automake help2man libtool
# awk
mpfr gmp gawk
# Base of the build environment
nproc whereis pkgconf fakeroot busybox sed grep cmake diffutils
# Compilers and runtimes
llvm-runtimes llvm22 llvm clang22 clang mlir flang22 openmp
# Binutils
flex bison jansson binutils
# Compression libraries
xz zlib-ng lz4 bzip2 brotli libarchive zstd gzip tar
# gettext and dependencies
libiconv libxml2 libunistring ncurses gettext less
# Base libraries
icu expat openssl libffi nghttp2 libidn2 readline sqlite libuv gtest
# Python and meson
python3 python3-installer python3-gpep517 python3-flit-core python3-packaging python3-setuptools python3-pyproject-hooks python3-wheel python3-build meson ninja
# More libraries that uses meson/cmake
publicsuffix-list libpsl libtasn1
# Tools that are used during system bootstrap/setup or basic system administration
bc scdoc bash nano lua5.4 sudo apk-tools abuild openssh curl p11-kit pax-utils
# Misc
ca-certificates build-base

# Rebuild these after bootstrap is complete
libiconv
