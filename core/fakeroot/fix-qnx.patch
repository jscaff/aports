diff -Naur fakeroot-1.38.1.orig/communicate.c fakeroot-1.38.1/communicate.c
--- fakeroot-1.38.1.orig/communicate.c	2026-06-11 15:33:54.000000000 +0000
+++ fakeroot-1.38.1/communicate.c	2026-06-11 15:34:04.000000000 +0000
@@ -32,6 +32,9 @@
 # endif
 #endif
 
+# ifdef __QNX__
+#define SOL_TCP 6
+# endif
 #include "communicate.h"
 #include <dlfcn.h>
 #include <stdio.h>
diff -Naur fakeroot-1.38.1.orig/faked.c fakeroot-1.38.1/faked.c
--- fakeroot-1.38.1.orig/faked.c	2026-06-11 15:33:54.000000000 +0000
+++ fakeroot-1.38.1/faked.c	2026-06-11 15:34:05.000000000 +0000
@@ -103,6 +103,7 @@
 #else /* FAKEROOT_FAKENET */
 # include <sys/socket.h>
 # include <sys/param.h>
+# include <sys/select.h>
 # include <netinet/in.h>
 # include <netinet/tcp.h>
 # include <arpa/inet.h>
@@ -1163,7 +1164,7 @@
       const int sd = sd_list_index(i);
 
       FD_SET(sd, &readfds);
-      maxfd = MAX(sd, maxfd);
+      maxfd = max(sd, maxfd);
     }
 
     count = select(maxfd + 1, &readfds, NULL, NULL, NULL);
diff -Naur fakeroot-1.38.1.orig/libfakeroot.c fakeroot-1.38.1/libfakeroot.c
--- fakeroot-1.38.1.orig/libfakeroot.c	2026-06-11 15:33:54.000000000 +0000
+++ fakeroot-1.38.1/libfakeroot.c	2026-06-11 15:34:05.000000000 +0000
@@ -1130,7 +1130,7 @@
 }
 
 #ifdef HAVE_FSTATAT
-#ifdef HAVE_MKNODAT
+#ifdef NEVER_HAVE_MKNODAT
 int WRAP_MKNODAT MKNODAT_ARG(int ver UNUSED,
 			     int dir_fd,
 			     const char *pathname,
@@ -1508,12 +1508,6 @@
   return pid;
 }
 
-pid_t vfork(void)
-{
-  /* We can't wrap vfork(2) without breaking everything... */
-  return fork();
-}
-
 /* Return an error when trying to close the comm_sd file descriptor
    (pretend that it's closed). */
 int close(int fd)
diff -Naur fakeroot-1.38.1.orig/message.h fakeroot-1.38.1/message.h
--- fakeroot-1.38.1.orig/message.h	2026-06-11 15:33:54.000000000 +0000
+++ fakeroot-1.38.1/message.h	2026-06-11 15:34:35.000000000 +0000
@@ -30,6 +30,8 @@
 # include <endian.h>
 #elif defined(HAVE_MACHINE_ENDIAN_H)
 # include <machine/endian.h>
+#elif defined(__QNX__)
+# include <sys/param.h>
 #else
 # error No endian header
 #endif
